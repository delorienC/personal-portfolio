<a href="{{ $link }}" target="_blank" class="inline-block">
    <img class="hidden lg:block" src="{{ asset('images/icons8-' . $name . '-96.png') }}" alt="{{ $name }}"
        class="hover:opacity-80">
    <img class="block lg:hidden"src="{{ asset('images/icons8-' . $name . '-48.png') }}" alt="{{ $name }}"
        class="hover:opacity-80">
</a>
