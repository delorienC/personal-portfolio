<div class="gsap-animate-experiences">
    <div class="w-full items-center justify-center">
        <div class="bg-white rounded-3xl p-0">
            <x-svg.progress-circle-96 progress="{{ $procent }}" iconlogo="{{ $experience }}" />
            <x-svg.progressbar-48 progress="{{ $procent }}" iconlogo="{{ $experience }}" />
        </div>
    </div>
</div>
